apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-config
  namespace: default
data:
  traefik.yml: |
    entryPoints:
      web:
        address: ":80"
      websecure:
        address: ":443"
    certificatesResolvers:
      myresolver:
        acme:
          email: chevannes.kai@gmail.com
          storage: acme.json
          dnsChallenge:
            provider: porkbun
            delayBeforeCheck: 0
